# NotifyHub Environment Configuration Example
#
# This file is a template. Copy it to .env and fill in the values for your environment.
# DO NOT COMMIT THE ACTUAL .env FILE! Add .env to your .gitignore.
#
# Docker Compose will automatically read variables from this .env file.

# --- RabbitMQ Credentials ---
# These are used by both the RabbitMQ container and the Spring app container.
RABBITMQ_USERNAME=guest
RABBITMQ_PASSWORD=guest

# --- Application Configuration ---
# The email address the application will use as the 'From' address
NOTIFYHUB_MAIL_FROM=noreply@example.com

# --- Mail Configuration (CHOOSE ONE OPTION) ---

# --- OPTION 1: Local Development/Testing with MailHog ---
# Use these settings to connect to the local MailHog container.
# MailHog requires no authentication.
# (Uncomment these 5 lines in your .env file to use MailHog)
#SPRING_MAIL_HOST=mailhog
#SPRING_MAIL_PORT=1025
#SPRING_MAIL_USERNAME=
#SPRING_MAIL_PASSWORD=
#SPRING_MAIL_PROPERTIES_MAIL_SMTP_AUTH=false
#SPRING_MAIL_PROPERTIES_MAIL_SMTP_STARTTLS_ENABLE=false

# ----------------------------------------------------

# --- OPTION 2: Generic Production SMTP Server (e.g., Gmail, SendGrid, etc.) ---
# Use these settings to connect to any real, secure mail server.
# Get these values from your email provider.
SPRING_MAIL_HOST=smtp.your-provider.com
SPRING_MAIL_PORT=the-port-number
SPRING_MAIL_USERNAME=your-username
SPRING_MAIL_PASSWORD=your-secret-password-or-api-key
SPRING_MAIL_PROPERTIES_MAIL_SMTP_AUTH=true
SPRING_MAIL_PROPERTIES_MAIL_SMTP_STARTTLS_ENABLE=true

