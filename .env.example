# Configuration for NotifyHub application using Docker Compose
# Copy this file to .env and replace placeholder values with your actual secrets/settings.
# DO NOT COMMIT THE ACTUAL .env FILE!

# These are used by the RabbitMQ container itself on first startup
# The default values used by the official image are guest/guest
RABBITMQ_USERNAME=guest
RABBITMQ_PASSWORD=guest

# The email address the application will use as the 'From' address
NOTIFYHUB_MAIL_FROM=noreply@example.com
